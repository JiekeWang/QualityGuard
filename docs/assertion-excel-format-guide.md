# Excel 断言配置完整指南

## 概述

本指南详细说明如何在 Excel 数据驱动文件中配置断言，支持**简化配置**和**完整配置**两种模式。

## 核心原则

✅ **简化优先** - 简单字段名 + 内容片段，系统自动智能匹配（推荐）
✅ **完整支持** - 深层嵌套路径 + 精确断言，适用于复杂场景
✅ **自动识别** - 系统根据字段名格式自动选择匹配模式
✅ **类型自动转换** - 自动识别数字、布尔值、null 等类型

---

## 🌟 配置模式对比

### 简化配置（推荐）⭐

**特点：** 维护成本最低，适用于大多数场景

| 列名 | 内容 | 说明 |
|------|------|------|
| `expected_PassedRules` | `"RuleName": "5.符合弹窗提醒-III类骨性关系","Output": "{\"Category\":\"侧位片\"` | 系统自动在响应中搜索 `PassedRules` 字段，智能匹配内容片段 |

**适用场景：**
- ✅ 字段在响应中唯一或首次出现即为目标
- ✅ 不需要精确匹配完整 JSON，只验证关键内容
- ✅ 数组中任一元素匹配即可

### 完整配置（精确控制）

**特点：** 精确指定路径，适用于需要严格控制的场景

| 列名 | 内容 | 说明 |
|------|------|------|
| `expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName` | `5.符合弹窗提醒-III类骨性关系` | 精确指定路径和数组索引 |

**适用场景：**
- ✅ 同名字段在响应中多次出现，需要指定具体位置
- ✅ 需要精确匹配字段值（而非部分匹配）
- ✅ 需要指定数组中的特定元素

---

## 字段命名规则

### 1. 基础格式

```
expected_<字段路径>[_<操作符>]
```

### 2. 路径表示法

使用**下划线**连接嵌套层级，**数字**表示数组索引：

| Excel 列名 | 自动生成的 JSONPath | 说明 |
|-----------|-------------------|------|
| `expected_user_id` | `$.data.user_id` | 简单字段（自动在 data 下查找） |
| `expected_status` | 状态码断言 | 特殊字段，断言 HTTP 状态码 |
| `expected_data_user_id` | `$.data.user_id` | 明确指定 data 层级 |
| `expected_items_0_name` | `$.items[0].name` | 数组第一个元素的 name 字段 |
| `expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName` | `$.ItemResultDict.OrthoDiagnosis.PassedRules[0].RuleName` | 深层嵌套路径 |

### 3. 支持的操作符后缀

| 后缀 | 操作符 | 说明 | 示例 |
|------|--------|------|------|
| （无后缀） | `equal` | 完全相等（默认） | `expected_age` = `25` |
| `_contains` | `contains` | 包含（字符串/数组） | `expected_message_contains` = `success` |
| `_greater_than` | `greater_than` | 大于 | `expected_age_greater_than` = `18` |
| `_less_than` | `less_than` | 小于 | `expected_price_less_than` = `1000` |
| `_not_equal` | `not_equal` | 不等于 | `expected_status_not_equal` = `error` |
| `_exists` | `exists` | 字段存在 | `expected_user_id_exists` = `true` |

---

## 实战示例

### 示例 1：简单响应断言

**响应数据：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "user_id": 1001,
    "username": "zhangsan",
    "age": 25
  }
}
```

**Excel 配置：**

| expected_status | expected_code | expected_message | expected_user_id | expected_username | expected_age_greater_than |
|----------------|---------------|------------------|-----------------|-------------------|--------------------------|
| 200 | 200 | success | 1001 | zhangsan | 18 |

**生成的断言：**
```json
[
  {"type": "status_code", "expected": 200},
  {"type": "json_path", "path": "$.code", "operator": "equal", "expected": 200},
  {"type": "json_path", "path": "$.message", "operator": "equal", "expected": "success"},
  {"type": "json_path", "path": "$.data.user_id", "operator": "equal", "expected": 1001},
  {"type": "json_path", "path": "$.data.username", "operator": "equal", "expected": "zhangsan"},
  {"type": "json_path", "path": "$.data.age", "operator": "greater_than", "expected": 18}
]
```

---

### 示例 2：数组响应断言

**响应数据：**
```json
{
  "data": {
    "items": [
      {"id": 1, "name": "产品A", "price": 99.9},
      {"id": 2, "name": "产品B", "price": 199.9}
    ],
    "total": 2
  }
}
```

**Excel 配置：**

| expected_items_0_id | expected_items_0_name | expected_items_0_price | expected_items_1_name_contains | expected_total |
|--------------------|-----------------------|------------------------|-------------------------------|----------------|
| 1 | 产品A | 99.9 | 产品 | 2 |

**生成的断言：**
```json
[
  {"type": "json_path", "path": "$.data.items[0].id", "operator": "equal", "expected": 1},
  {"type": "json_path", "path": "$.data.items[0].name", "operator": "equal", "expected": "产品A"},
  {"type": "json_path", "path": "$.data.items[0].price", "operator": "equal", "expected": 99.9},
  {"type": "json_path", "path": "$.data.items[1].name", "operator": "contains", "expected": "产品"},
  {"type": "json_path", "path": "$.data.total", "operator": "equal", "expected": 2}
]
```

---

### 示例 3：深层嵌套（咬合重建规则引擎）

**响应数据：**
```json
{
  "WorkflowName": "咬合重建-弹窗提醒",
  "WorkflowVersion": "13502",
  "ItemResultDict": {
    "OrthoDiagnosis": {
      "PassedRules": [
        {
          "RuleName": "5.符合弹窗提醒-III类骨性关系",
          "Output": "{\"Category\":\"侧位片\",\"Content\":\"III类骨性关系\"}"
        }
      ],
      "ErrorRules": []
    }
  }
}
```

#### 方式A：简化配置（推荐）⭐

**Excel 配置：**

| Codes | DamageToothCount | DamageAreaCount | expected_PassedRules |
|-------|------------------|----------------|---------------------|
| CI-lll-bony | 2 | 1 | "RuleName": "5.符合弹窗提醒-III类骨性关系","Output": "{\"Category\":\"侧位片\",\"Content\":\"III类骨性关系 |

**生成的断言：**
```json
[
  {
    "type": "smart_match",
    "field": "PassedRules",
    "expected": "\"RuleName\": \"5.符合弹窗提醒-III类骨性关系\",\"Output\": \"{\\\"Category\\\":\\\"侧位片\\\",\\\"Content\\\":\\\"III类骨性关系\""
  }
]
```

**工作原理：**
1. 系统在响应中**递归搜索** `PassedRules` 字段
2. 找到后进行**智能匹配**：
   - 如果是数组，检查任一元素是否匹配
   - 对内容进行**部分匹配**（不需要完整 JSON）
   - 忽略空格和换行差异

**优点：**
- ✅ **维护简单**：只需要字段名和关键内容片段
- ✅ **容错性强**：不受 JSON 格式、空格、字段顺序影响
- ✅ **自动处理数组**：数组中任一元素匹配即通过

#### 方式B：完整配置（精确控制）

**Excel 配置：**

| expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName | expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_Output_contains |
|---------------------------------------------------------------|---------------------------------------------------------------------|
| 5.符合弹窗提醒-III类骨性关系 | III类骨性关系 |

**生成的断言：**
```json
[
  {
    "type": "json_path",
    "path": "$.ItemResultDict.OrthoDiagnosis.PassedRules[0].RuleName",
    "operator": "equal",
    "expected": "5.符合弹窗提醒-III类骨性关系"
  },
  {
    "type": "json_path",
    "path": "$.ItemResultDict.OrthoDiagnosis.PassedRules[0].Output",
    "operator": "contains",
    "expected": "III类骨性关系"
  }
]
```

**优点：**
- ✅ **精确控制**：明确指定路径和数组索引
- ✅ **多字段拆分**：每个字段单独断言，错误定位更精准
- ✅ **灵活操作符**：可以对不同字段使用不同的操作符

**选择建议：**
- 🌟 **大多数情况用方式A**：简单、快速、容错性强
- 🎯 **需要精确控制时用方式B**：多个同名字段、特定数组元素、精确匹配

---

## Excel 表格完整示例

### 咬合重建规则引擎测试数据

| Codes | DamageToothCount | DamageAreaCount | expected_WorkflowName | expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName | expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_Output_contains |
|-------|------------------|----------------|----------------------|---------------------------------------------------------------|---------------------------------------------------------------------|
| CI-Il-bony | 1 | 1 | 咬合重建-弹窗提醒 | 4.符合弹窗提醒-II类骨性关系 | II类骨性关系 |
| CI-lll-bony | 2 | 1 | 咬合重建-弹窗提醒 | 5.符合弹窗提醒-III类骨性关系 | III类骨性关系 |
| DI-5-tooth | 5 | 3 | 咬合重建-弹窗提醒 | 6.符合弹窗提醒-5颗及以上缺牙 | 缺牙 |

**说明：**
- 前3列是**请求参数**（会替换到请求模板中）
- 后3列是**断言配置**（`expected_` 开头）
- 每行数据独立执行一次完整的请求和断言

---

## 高级技巧

### 1. 组合使用多种操作符

```excel
| expected_age_greater_than | expected_age_less_than |
|--------------------------|------------------------|
| 18                       | 60                     |
```
验证年龄在 18-60 之间。

### 2. 验证数组长度

```excel
| expected_items_0_id | expected_items_1_id | expected_items_2_id |
|--------------------|--------------------|--------------------|
| 1                  | 2                  | 3                  |
```
同时验证数组至少有3个元素，且前3个元素的ID正确。

### 3. 部分字段断言

不需要断言所有字段，只配置关键字段即可：

```excel
| expected_status | expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName |
|----------------|---------------------------------------------------------------|
| 200            | 4.符合弹窗提醒-II类骨性关系                                      |
```

### 4. 混合精确匹配和模糊匹配

```excel
| expected_user_id | expected_username | expected_email_contains |
|-----------------|-------------------|------------------------|
| 1001            | zhangsan         | @example.com           |
```

---

## 常见问题

### Q1: 为什么我的断言总是失败？

**检查清单：**
1. ✅ 路径是否正确？使用浏览器开发者工具查看实际响应结构
2. ✅ 期望值类型是否匹配？数字不要加引号
3. ✅ 数组索引是否正确？从 0 开始
4. ✅ 字符串匹配是否区分大小写？考虑使用 `_contains`

### Q2: 如何验证某个字段不存在？

暂不直接支持，但可以通过断言父级对象实现：

```excel
| expected_node_data |
|-------------------|
| {"user_id": 1001} |
```

这会验证 `data` 节点只包含 `user_id` 字段。

### Q3: 如何断言嵌套很深的字段？

使用下划线连接完整路径：

```excel
| expected_level1_level2_level3_level4_field |
|-------------------------------------------|
| value                                     |
```

会生成：`$.level1.level2.level3.level4.field`

### Q4: JSON 字符串字段如何断言？

**推荐使用 `contains`：**
```excel
| expected_Output_contains |
|-------------------------|
| 关键内容                  |
```

**精确匹配需要转义：**
```excel
| expected_Output |
|----------------|
| {"Category":"侧位片","Content":"II类骨性关系"} |
```
注意：JSON 格式必须完全一致（空格、顺序等）。

---

## 调试技巧

### 1. 查看生成的断言

测试执行日志会显示：
```
== 断言配置检查 ==
使用测试数据中的断言配置，数量: 2
断言配置详情:
  [1] {"type": "json_path", "path": "$.ItemResultDict...", ...}
  [2] {"type": "json_path", "path": "$.ItemResultDict...", ...}
```

### 2. 逐步增加断言

先只配置一个最简单的断言：
```excel
| expected_status |
|----------------|
| 200            |
```

成功后再逐步添加其他字段断言。

### 3. 使用 Postman 验证响应结构

在 Postman 中先手动测试接口，确认响应结构后再配置 Excel。

---

## 最佳实践

### 1. 优先使用简化配置 ⭐

```excel
| expected_PassedRules | expected_WorkflowName |
|---------------------|----------------------|
| "RuleName": "规则1" | 咬合重建-弹窗提醒 |
```

而不是：

```excel
| expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName | expected_WorkflowName |
|---------------------------------------------------------------|----------------------|
| 规则1 | 咬合重建-弹窗提醒 |
```

**原因：** 简化配置更易维护，且容错性更强。

### 2. 只维护关键内容片段

对于复杂的 JSON 字段，不需要写完整内容：

```excel
| expected_PassedRules |
|---------------------|
| "Content":"III类骨性关系" |
```

而不是完整的 JSON 字符串。

### 3. 何时使用完整配置

- 同名字段在响应中多次出现
- 需要断言数组中的多个不同元素
- 需要对不同字段使用不同操作符

### 4. 保持列名可读

- ✅ 好：`expected_PassedRules`
- ❌ 差：`expected_ItemResultDict_OrthoDiagnosis_PassedRules_0_RuleName`

### 5. 版本管理 Excel 文件

数据驱动文件也是测试资产，应纳入版本控制。

---

## 总结

### 🌟 两种配置模式

1. **简化配置（推荐）**：`expected_<简单字段名>` + 内容片段
   - 系统自动在响应中搜索字段
   - 智能匹配内容（部分匹配、数组自动处理）
   - 维护成本最低，容错性最强

2. **完整配置（精确控制）**：`expected_<完整路径>` + 精确值
   - 明确指定 JSONPath 路径
   - 支持多种操作符
   - 适用于需要精确控制的场景

### ✅ 系统自动化能力

无需手写 JSON 配置，系统会自动：

✅ 识别配置模式（简化 vs 完整）
✅ 递归搜索字段（简化模式）
✅ 推断 JSONPath 路径（完整模式）
✅ 智能匹配内容（部分匹配、数组处理）
✅ 识别操作符后缀
✅ 转换数据类型

### 🎯 选择建议

| 场景 | 推荐模式 | 示例 |
|------|---------|------|
| 一般业务验证 | 简化配置 | `expected_PassedRules` |
| 多个同名字段 | 完整配置 | `expected_data_items_0_name` |
| 复杂 JSON 字段 | 简化配置 | 内容片段即可 |
| 精确值验证 | 完整配置 | 配合操作符使用 |

这种设计在**易用性**、**灵活性**和**功能完整性**之间取得了最佳平衡，非常适合数据驱动测试场景。

